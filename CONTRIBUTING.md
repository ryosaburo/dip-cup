# コミット・プッシュ時の注意点（共同作業者向け）

複数人で並行して同じリポジトリを触るため、コミット・プッシュの際に事故が起きやすいポイントをまとめる。`README.md`のブランチ運用ルールと合わせて確認すること。

## 作業を始める前に

- 必ず最新の`dev`から新しいブランチ（`feature/<内容>` / `fix/<内容>`）を切る。古い`dev`から分岐すると、後で無駄なコンフリクトが増える。
- 他人が触っていそうなファイル・機能（例: `PlayingCard.tsx`のような共通UIコンポーネント）を変更する場合は、着手前に一声かける。実際にこのリポジトリでも同じファイルへの同時変更でコンフリクトが発生している。

## コミットする前に

- `git status`で差分ファイルを確認してから`git add`する。`git add -A`や`git add .`は使わず、必要なファイルだけを明示的にステージする（意図しないファイルの混入を防ぐため）。
- ローカルで以下を実行し、全て通ることを確認してからコミットする（pre-commitフックは設定されていないため、各自の責任で行う）。
  ```bash
  npm run build -w @battle/shared
  npx --prefix packages/server tsc --noEmit
  npx --prefix packages/frontend tsc --noEmit
  npm run lint -w @battle/frontend
  npm test
  ```
- コミットメッセージは日本語で、「何をしたか」より「なぜ変更したか」が伝わるように書く。1コミット1テーマを意識し、無関係な変更を混ぜない。
- `.env` / `.env.local`など、Supabaseのキーや秘密情報を含みうるファイルは絶対にコミットしない。`git status`で見慣れないファイルが混ざっていないか必ず確認する。

## プッシュする前に

- プッシュ直前に`git fetch`し、リモートの同じブランチが自分の知らない間に進んでいないか確認する（このリポジトリはPRマージやブランチ間の同期マージが頻繁に発生する）。
- リモートが進んでいた場合は`git rebase origin/<ブランチ名>`（または`git merge`）で追従してから、改めてビルド・テストをやり直してプッシュする。
- `main`・`dev`など共有ブランチへの`push --force`は行わない。個人の作業ブランチであっても、他人が既にpullしている可能性がある場合は避け、必要な場合は事前に共有する。
- `--no-verify`などでフックやチェックをスキップしない。

## コンフリクトが起きたら

- 片方の変更をそのまま採用する（`--ours`/`--theirs`で機械的に解決する）のではなく、両方の変更の意図を読んでから手動で統合する。例えば`PlayingCard.tsx`のケースでは、片方が追加した「カードサイズのカスタマイズ機能」と、もう片方が追加した「カード名表示用のlabelプロパティ」の両方を残す形で解決した。
- 解決後は必ずビルド・型チェック・Lint・テストを再実行し、双方の変更が壊れていないことを確認する。

## PRを出すとき

- `dev`・`main`宛のPRでは`.github/workflows/ci.yml`のCI（sharedビルド／server・frontendの型チェック／frontendのLint／serverのユニットテスト／frontendの本番ビルド）が自動実行される。`build-and-test`がRequired status checkのため、CIが赤いままではマージできない。手元で上記コマンドを流しておけば、CIで初めて失敗に気づく事態を防げる。

## ローカル開発時の注意（マルチプレイ動作確認）

- サーバーはルーム状態をすべてin-memoryで保持しており、永続化されない。`packages/shared`を変更すると`tsx watch`がサーバーを自動再起動し、その時点で進行中のルーム・対戦状態は消える。2タブでの手動E2E確認中に`shared`側を編集した場合は、両タブとも作り直し（ルーム再作成・再入室）が必要になる。
